# Comment Modes# Comment Modes



Ce projet supporte **2 modes** pour poster les commentaires de review IA sur GitLab. Configurez le mode via la variable d'environnement `COMMENT_MODE`.Ce projet supporte 2 modes différents pour poster les commentaires de review IA sur GitLab. Configurez le mode via la variable d'environnement `COMMENT_MODE`.



## 🌍 Mode `global` (défaut)## 🌍 Mode `global` (défaut)



**Comportement classique** : Un seul commentaire global contenant toute la review.**Comportement classique** : Un seul commentaire global contenant toute la review.



```bash```bash

COMMENT_MODE=globalCOMMENT_MODE=global

``````



**Avantages :****Avantages :**

- ✅ Simple et fiable- Simple et fiable

- ✅ Fonctionne toujours (pas besoin de SHAs de commits)- Fonctionne toujours (pas besoin de SHAs de commits)

- ✅ Facile à lire d'un coup d'œil- Facile à lire d'un coup d'œil

- ✅ Pas de parsing nécessaire- Pas de parsing nécessaire

- ✅ Parfait pour débuter

**Inconvénients :**

**Inconvénients :**- Commentaires non liés au code spécifique

- ❌ Commentaires non liés au code spécifique- Difficile de retrouver la ligne concernée

- ❌ Difficile de retrouver la ligne concernée dans de gros diffs

**Exemple de sortie :**

**Exemple de sortie :**```

```markdown📝 AI Code Review

📝 AI Code Review

1. Summary of changes:

## 1. Summary of changes   - Added new feature X

- Added new authentication middleware   - Fixed bug in module Y

- Implemented JWT token validation

- Added user role-based access control2. Code clarity: Generally good



## 2. Code clarity3. Potential issues:

The code is generally well-structured. Variable names are descriptive.   - In file.ts line 42: Missing null check

   - In service.ts line 89: Consider async/await

## 3. Potential issues

- In file.ts line 42: Missing null check before accessing user.role[... reste de la review ...]

- In service.ts line 89: Consider using async/await for better error handling```



## 4. Security concerns---

- Password is logged in plain text at line 23 of auth/login.ts

## 📊 Mode `structured`

---

*This comment was generated by an artificial intelligence duck.***Format JSON strict** pour un parsing fiable et des commentaires inline précis.

```

```bash

---COMMENT_MODE=structured

```

## 📊 Mode `structured`

**Comment ça marche :**

**Format JSON strict** pour un parsing fiable et des commentaires inline précis.- L'IA reçoit des instructions strictes pour générer un JSON structuré

- Le système parse le JSON avec `JSON.parse()` (simple et fiable)

```bash- Les commentaires inline sont postés sur les lignes spécifiques

COMMENT_MODE=structured- Le résumé est posté comme commentaire global

```

**Format JSON attendu :**

**Comment ça marche :**```json

1. L'IA reçoit des instructions strictes pour générer un JSON structuré{

2. Le système parse le JSON avec `JSON.parse()` (simple et fiable ✅)  "summary": "Overall review summary with general observations (optional)",

3. Les commentaires inline sont postés sur les lignes spécifiques du code  "inline_comments": [

4. Le résumé est posté comme commentaire global    {

      "file": "src/services/user.ts",

**Format JSON attendu :**      "line": 42,

```json      "comment": "This variable should be const instead of let"

{    },

  "summary": "Overall review summary with general observations (optional)",    {

  "inline_comments": [      "file": "src/api/routes.ts",

    {      "line": 89,

      "file": "src/services/user.ts",      "comment": "Consider adding error handling here"

      "line": 42,    }

      "comment": "⚠️ Missing null check - This could throw TypeError if user is undefined"  ]

    },}

    {```

      "file": "src/api/routes.ts",

      "line": 89,**Avantages :**

      "comment": "💡 Consider adding error handling with try/catch here"- ✅ Parsing simple et fiable avec `JSON.parse()`

    }- ✅ Pas de risque d'erreur de regex

  ]- ✅ Séparation claire entre résumé et commentaires inline

}- ✅ Commentaires inline précis sur le code

```- ✅ Résumé global pour le contexte



**Avantages :****Inconvénients :**

- ✅ **Parsing simple et fiable** avec `JSON.parse()` (pas de regex complexes)- Nécessite les SHAs de commits (fournis automatiquement par le webhook)

- ✅ Pas de risque d'erreur d'interprétation- L'IA doit générer un JSON valide (mais géré avec fallback)

- ✅ Séparation claire entre résumé et commentaires inline

- ✅ **Commentaires inline précis** directement sur le code**Prompt reçu par l'IA :**

- ✅ Résumé global pour le contexte général```

- ✅ Flexible : peut poster uniquement inline, uniquement résumé, ou les deux**IMPORTANT: You MUST respond with a valid JSON object in this exact format:**



**Inconvénients :**```json

- ⚠️ Nécessite les SHAs de commits (fournis automatiquement par le webhook){

- ⚠️ L'IA doit générer un JSON valide (mais fallback automatique si erreur)  "summary": "Your overall review summary here...",

  "inline_comments": [

**Fallback automatique :**    {

- Si JSON invalide → Texte complet utilisé comme résumé global      "file": "path/to/file.ts",

- Si SHAs manquants → Seul le résumé global est posté      "line": 42,

      "comment": "Your specific comment about this line"

**Prompt reçu par l'IA :**    }

```  ]

**IMPORTANT: You MUST respond with a valid JSON object in this exact format:**}

```

```json

{**Rules:**

  "summary": "Your overall review summary here...",- The JSON must be valid (no trailing commas, proper quotes)

  "inline_comments": [- The "summary" field contains your general review (can be empty)

    {- The "inline_comments" array contains specific issues found

      "file": "path/to/file.ts",- Each inline comment MUST have: "file", "line", "comment"

      "line": 42,```

      "comment": "Your specific comment about this line"

    }**Rendu GitLab :**

  ]- **Commentaire global :** "Overall review summary with general observations..."

}- **Inline** sur `src/services/user.ts:42` : "This variable should be const instead of let"

```- **Inline** sur `src/api/routes.ts:89` : "Consider adding error handling here"



**Rules:**---

- The JSON must be valid (no trailing commas, proper quotes)

- The "summary" field contains your general review (can be empty)## 🔧 Prérequis techniques

- The "inline_comments" array contains specific issues found

- Each inline comment MUST have: "file", "line", "comment"### Pour le mode `structured`

- If no specific issues: "inline_comments": []

```Ce mode nécessite les **commit SHAs** pour créer les positions de diff GitLab :

- `base_sha` : SHA du commit de base (target branch)

**Rendu dans GitLab :**- `head_sha` : SHA du commit HEAD (source branch)

- `start_sha` : SHA de départ (généralement = base_sha)

**Commentaire global** (notes API) :

```Ces SHAs sont automatiquement extraits du webhook GitLab lors des événements :

📝 AI Code Review Summary- `merge_request.open`

- `merge_request.update`

Overall, good implementation of authentication with JWT.- `merge_request.reopen`

The code is generally well-structured, but there are a few

security concerns and potential bugs that need attention.Si les SHAs sont manquants, seul le résumé global est posté (fallback automatique).



## Recommendations### API GitLab utilisée

1. Add integration tests for the auth flow

2. Document the JWT token structure- **Global comment** : `POST /projects/:id/merge_requests/:iid/notes`

3. Consider rate limiting on login endpoint- **Inline comment** : `POST /projects/:id/merge_requests/:iid/discussions` avec objet `position`



------

*This comment was generated by an artificial intelligence duck.*

```## 📈 Recommandations



**Commentaires inline** (discussions API) :| Use Case | Mode recommandé | Raison |

|----------|----------------|--------|

Sur `src/services/user.ts` ligne 42 :| **Petite équipe, MR simples** | `global` | Simple, fiable, pas de setup |

```| **Review détaillées, gros diffs** | `structured` | Vue d'ensemble + détails inline |

⚠️ Missing null check - This could throw TypeError if user is undefined| **Workflow strict, code review formelle** | `structured` | Format contrôlé, parsing fiable |

| **Premiers tests** | `global` | Commencer simple |

Suggested fix:| **Maximum de détails sur le code** | `structured` | Commentaires inline précis |

```typescript

if (!user || !user.role) {**Recommandation générale : `structured`** pour bénéficier des commentaires inline tout en gardant un parsing simple et fiable.

  throw new UnauthorizedError('Invalid user');

}---

```

```## 🐛 Debugging



Sur `src/api/routes.ts` ligne 89 :Si les commentaires inline ne s'affichent pas :

```

💡 Consider adding error handling with try/catch here1. **Vérifier les SHAs** : Consultez les logs pour voir si `baseSha`, `headSha`, `startSha` sont présents

```   ```

   Posting inline comments: {

---     url: '...',

     commentCount: 3,

## 🔧 Prérequis techniques     baseSha: 'abc123...',

     headSha: 'def456...'

### Pour le mode `structured`   }

   ```

Ce mode nécessite les **commit SHAs** pour créer les positions de diff GitLab :

- `base_sha` : SHA du commit de base (target branch)2. **Vérifier le JSON généré** : Vérifiez que l'IA génère bien un JSON valide

- `head_sha` : SHA du commit HEAD (source branch)   ```json

- `start_sha` : SHA de départ (= base_sha)   {

     "summary": "...",

**Ces SHAs sont automatiquement extraits** du webhook GitLab lors des événements :     "inline_comments": [...]

- `merge_request.open`   }

- `merge_request.update`   ```

- `merge_request.reopen`

3. **Vérifier les erreurs GitLab API** : Les logs montrent les erreurs HTTP détaillées

✅ **Aucune configuration manuelle nécessaire !**   ```

   Failed to post inline comment on src/file.ts:42: HTTP 400 Bad Request

Si les SHAs sont manquants (cas rare), le système fallback automatiquement sur un commentaire global uniquement.   Response body: {"message":"Invalid position"}

   ```

### API GitLab utilisée

4. **Fallback automatique** : 

| Type de commentaire | Endpoint GitLab |   - Si le JSON est invalide → Le texte complet est utilisé comme résumé

|---------------------|----------------|   - Si les SHAs manquent → Seul le résumé global est posté

| **Global comment** | `POST /projects/:id/merge_requests/:iid/notes` |   ```

| **Inline comment** | `POST /projects/:id/merge_requests/:iid/discussions` |   Failed to parse structured JSON response: SyntaxError

   Missing commit SHAs, cannot post inline comments

---   Posting full response as global comment

   ```

## 📈 Comparaison et recommandations

---

| Critère | `global` | `structured` |

|---------|----------|--------------|## 🚀 Exemples de configuration

| **Simplicité d'utilisation** | ⭐⭐⭐⭐⭐ Très simple | ⭐⭐⭐⭐ Simple |

| **Fiabilité** | ⭐⭐⭐⭐⭐ 100% fiable | ⭐⭐⭐⭐ Très fiable |### Configuration minimaliste (débutant)

| **Parsing** | ❌ Aucun | ✅ JSON.parse() |```bash

| **Commentaires inline** | ❌ Non | ✅ Oui |COMMENT_MODE=global

| **Vue d'ensemble** | ✅ Oui | ✅ Oui |AI_MODEL=claude-3-5-sonnet-20241022

| **Prérequis** | ✅ Aucun | ⚠️ SHAs (auto) |```

| **Flexibilité** | ⭐⭐ Limité | ⭐⭐⭐⭐⭐ Très flexible |

### Configuration recommandée (production)

### 🎯 Quand utiliser quel mode ?```bash

COMMENT_MODE=structured

| Use Case | Mode recommandé | Raison |AI_MODEL=claude-3-5-sonnet-20241022

|----------|----------------|--------|```

| **Découverte / premiers tests** | `global` | Simple, zéro config |

| **Petites MRs (< 5 fichiers)** | `global` | Suffisant, pas besoin de complexité |### Configuration avancée (GPT-5)

| **Grosses MRs (> 5 fichiers)** | `structured` | Inline comments facilitent la review |```bash

| **Code reviews formelles** | `structured` | Format contrôlé, traçabilité |COMMENT_MODE=structured

| **Équipe expérimentée** | `structured` | Meilleure expérience |AI_MODEL=gpt-5

| **CI/CD automatisé** | `structured` | Parsing fiable, format prévisible |```



**💡 Recommandation générale : `structured`**---



Le mode `structured` offre le meilleur des deux mondes :## 🔄 Migration depuis l'ancienne version

- Commentaires inline précis sur le code

- Résumé global pour le contexteSi vous utilisez déjà ce projet, **rien à faire** ! Le mode par défaut est `global`, qui correspond au comportement actuel.

- Parsing JSON simple et fiable

- Flexible (inline seul, résumé seul, ou les deux)Pour activer le mode structuré :

1. Ajoutez `COMMENT_MODE=structured` dans votre `.env`

---2. Redéployez l'application

3. Testez avec une nouvelle MR

## 💡 Pourquoi seulement 2 modes ?

Pas besoin de changer les webhooks GitLab existants.

### Simplicité et fiabilité

---

Au départ, ce projet supportait 4 modes (`global`, `inline`, `hybrid`, `structured`). Nous avons simplifié pour ces raisons :

## 💡 Pourquoi 2 modes seulement ?

1. **Parsing JSON >> Parsing regex**

   - `JSON.parse()` est simple, rapide, et fiable**Simplicité et fiabilité** :

   - Les regex de patterns texte sont fragiles et peuvent échouer- Le parsing JSON est beaucoup plus simple et fiable que le parsing de patterns texte avec regex

- Un seul format structuré permet de gérer tous les cas (inline uniquement, résumé uniquement, ou les deux)

2. **Un seul format structuré = moins de code**- Moins de code = moins de bugs

   - Le mode `structured` peut tout faire :- Plus facile à maintenir et débugger

     - Inline uniquement : `"summary": "", "inline_comments": [...]`

     - Résumé uniquement : `"summary": "...", "inline_comments": []`Le mode `structured` est flexible :

     - Les deux : Remplir les deux champs- **Inline uniquement** : `"summary": ""` et remplir `inline_comments`

   - Pas besoin de 3 modes séparés !- **Résumé uniquement** : `"inline_comments": []` et remplir `summary`

- **Les deux** : Remplir les deux champs

3. **Maintenance simplifiée**

   - Moins de code = moins de bugs## 🌍 Mode `global` (défaut)

   - Plus facile à débugger

   - Plus facile à documenter**Comportement classique** : Un seul commentaire global contenant toute la review.



4. **Meilleure expérience développeur**```bash

   - Un seul format à apprendreCOMMENT_MODE=global

   - Comportement prévisible```

   - Fallback automatique en cas d'erreur

**Avantages :**

---- Simple et fiable

- Fonctionne toujours (pas besoin de SHAs de commits)

## 🐛 Debugging- Facile à lire d'un coup d'œil



### Les commentaires inline ne s'affichent pas**Inconvénients :**

- Commentaires non liés au code spécifique

**1. Vérifier les SHAs dans les logs**- Difficile de retrouver la ligne concernée



```**Exemple de sortie :**

Posting inline comments: {```

  url: 'https://gitlab.com/api/v4/projects/12345/merge_requests/42/discussions',📝 AI Code Review

  commentCount: 3,

  baseSha: 'abc123...',1. Summary of changes:

  headSha: 'def456...'   - Added new feature X

}   - Fixed bug in module Y

```

2. Code clarity: Generally good

✅ Si les SHAs sont présents → OK  

❌ Si absents → Le système devrait fallback automatiquement sur résumé global3. Potential issues:

   - In file.ts line 42: Missing null check

**2. Vérifier le JSON généré par l'IA**   - In service.ts line 89: Consider async/await



Regardez les logs pour voir la réponse de l'IA. Elle devrait contenir :[... reste de la review ...]

```

```json

{---

  "summary": "...",

  "inline_comments": [## 📍 Mode `inline`

    {"file": "...", "line": 42, "comment": "..."}

  ]**Commentaires uniquement sur les lignes de code** concernées. Pas de commentaire global.

}

``````bash

COMMENT_MODE=inline

❌ Si le JSON est invalide (trailing comma, quotes mal échappées) :```

```

Failed to parse structured JSON response: SyntaxError: Unexpected token**Comment ça marche :**

Posting full response as global comment- L'IA est invitée à formater ses commentaires avec le pattern : `In \`path/to/file.ts\` line 42: comment`

```- Le système parse ces patterns et crée des discussions GitLab sur les lignes spécifiques

- Les observations générales sont ignorées

**3. Vérifier les erreurs GitLab API**

**Avantages :**

Les logs montrent maintenant les erreurs HTTP détaillées :- Commentaires directement sur le code concerné

- Meilleure visibilité dans le diff GitLab

```- Facile de résoudre les discussions une par une

Failed to post inline comment on src/file.ts:42: HTTP 400 Bad Request

Response body: {"message":"Invalid position: line is out of range"}**Inconvénients :**

```- Nécessite les SHAs de commits (fournis par le webhook)

- Pas de vue d'ensemble globale

**Erreurs courantes :**- L'IA doit bien respecter le format

- `400 Bad Request` : Position de diff invalide (ligne hors du diff)

- `401 Unauthorized` : Token GitLab invalide**Format attendu de l'IA :**

- `404 Not Found` : MR ou projet inexistant```

- `422 Unprocessable Entity` : SHAs obsolètes (MR a été mise à jour)In `src/services/user.ts` line 42: This variable should be const instead of let

In `src/api/routes.ts` line 89: Consider adding error handling here

**4. Solutions**```



| Problème | Solution |**Rendu GitLab :**

|----------|----------|- Commentaire inline sur `src/services/user.ts` ligne 42

| SHAs manquants | Vérifier que le webhook envoie les données complètes |- Commentaire inline sur `src/api/routes.ts` ligne 89

| JSON invalide | L'IA utilise automatiquement le texte comme résumé global |

| Ligne hors du diff | L'IA a référencé une ligne non modifiée (améliorer le prompt) |---

| Token invalide | Régénérer un token GitLab avec scope `api` |

## 🎯 Mode `hybrid`

---

**Le meilleur des deux mondes** : Commentaires inline + résumé global.

## 🚀 Configuration

```bash

### Configuration minimaliste (débutant)COMMENT_MODE=hybrid

```

```bash

# .env**Comment ça marche :**

GITLAB_TOKEN=<votre-token>- Même parsing que le mode `inline` pour extraire les commentaires spécifiques

GITLAB_URL=https://gitlab.com/api/v4- Le reste du texte (résumé, observations générales) est posté comme commentaire global

AI_MODEL=claude-3-5-sonnet-20241022- Vous obtenez les deux : discussions sur le code ET vue d'ensemble

COMMENT_MODE=global

```**Avantages :**

- Commentaires inline pour les problèmes spécifiques

### Configuration recommandée (production)- Résumé global pour le contexte

- Meilleure expérience utilisateur

```bash

# .env**Inconvénients :**

GITLAB_TOKEN=<votre-token>- Nécessite les SHAs de commits

GITLAB_URL=https://gitlab.com/api/v4- Plus de commentaires à gérer

AI_MODEL=claude-3-5-sonnet-20241022

COMMENT_MODE=structured  # ⭐ Recommandé**Exemple de sortie IA :**

``````

General Review Summary:

### Configuration avancée (GPT-5)Overall, the code quality is good. The changes introduce a new authentication system.



```bashIn `src/auth/login.ts` line 23: Missing input validation for email format

# .envIn `src/auth/token.ts` line 45: Token expiration should be configurable

GITLAB_TOKEN=<votre-token>

GITLAB_URL=https://gitlab.com/api/v4All tests are passing. Consider adding integration tests for the auth flow.

AI_MODEL=gpt-5```

COMMENT_MODE=structured

```**Rendu GitLab :**

- **Commentaire global :** "General Review Summary: Overall, the code quality is good... All tests are passing..."

---- **Inline** sur `src/auth/login.ts:23` : "Missing input validation for email format"

- **Inline** sur `src/auth/token.ts:45` : "Token expiration should be configurable"

## 🔄 Migration

---

### Depuis la version 1.x

## 📊 Mode `structured`

**Aucune action requise !** 🎉

**Format JSON strict** pour une parsing précise et contrôlée.

Le mode par défaut est `global`, qui correspond au comportement de la v1.x.

```bash

Pour activer les commentaires inline :COMMENT_MODE=structured

```

1. Ouvrez votre fichier `.env`

2. Ajoutez ou modifiez : `COMMENT_MODE=structured`**Comment ça marche :**

3. Redéployez l'application- L'IA reçoit des instructions strictes pour générer un JSON structuré

4. Créez une nouvelle MR pour tester- Le système parse le JSON pour extraire le résumé et les commentaires inline

- Fallback sur le parsing de patterns si le JSON est invalide

**Pas besoin de :**

- ❌ Changer les webhooks GitLab**Format JSON attendu :**

- ❌ Modifier les tokens```json

- ❌ Reconfigurer quoi que ce soit{

  "summary": "Overall review summary with general observations",

---  "inline_comments": [

    {

## 📚 Ressources      "file": "src/services/user.ts",

      "line": 42,

- [EXAMPLES.md](./EXAMPLES.md) - Exemples concrets de sorties pour chaque mode      "comment": "This variable should be const instead of let"

- [GITLAB_API.md](./GITLAB_API.md) - Documentation complète de l'API GitLab    },

- [ARCHITECTURE.md](./ARCHITECTURE.md) - Architecture technique détaillée    {

- [README.md](./README.md) - Guide principal du projet      "file": "src/api/routes.ts",

      "line": 89,
      "comment": "Consider adding error handling here"
    }
  ]
}
```

**Avantages :**
- Parsing précis et fiable
- Pas de risque d'erreur de format
- Séparation claire entre résumé et commentaires inline

**Inconvénients :**
- Nécessite les SHAs de commits
- L'IA peut avoir du mal à respecter le format JSON strict
- Légèrement plus verbeux

**Prompt reçu par l'IA :**
```
Please provide your review in the following JSON format:
{
  "summary": "Overall review summary",
  "inline_comments": [
    { "file": "path/to/file.ts", "line": 42, "comment": "..." }
  ]
}
```

---

## 🔧 Prérequis techniques

### Pour les modes `inline`, `hybrid` et `structured`

Ces modes nécessitent les **commit SHAs** pour créer les positions de diff GitLab :
- `base_sha` : SHA du commit de base (target branch)
- `head_sha` : SHA du commit HEAD (source branch)
- `start_sha` : SHA de départ (généralement = base_sha)

Ces SHAs sont automatiquement extraits du webhook GitLab lors des événements :
- `merge_request.open`
- `merge_request.update`
- `merge_request.reopen`

Si les SHAs sont manquants, le système fallback automatiquement sur un commentaire global.

### API GitLab utilisée

- **Global comment** : `POST /projects/:id/merge_requests/:iid/notes`
- **Inline comment** : `POST /projects/:id/merge_requests/:iid/discussions` avec objet `position`

---

## 📈 Recommandations

| Use Case | Mode recommandé | Raison |
|----------|----------------|--------|
| **Petite équipe, MR simples** | `global` | Simple, fiable |
| **Review détaillées, gros diffs** | `hybrid` | Vue d'ensemble + détails |
| **Workflow strict, code review formelle** | `structured` | Format contrôlé |
| **Maximum de détails sur le code** | `inline` | Tout inline |
| **Premiers tests** | `global` | Commencer simple |

---

## 🐛 Debugging

Si les commentaires inline ne s'affichent pas :

1. **Vérifier les SHAs** : Consultez les logs pour voir si `baseSha`, `headSha`, `startSha` sont présents
   ```
   Posting inline comments: {
     url: '...',
     commentCount: 3,
     baseSha: 'abc123...',
     headSha: 'def456...'
   }
   ```

2. **Vérifier le format des commentaires** : En mode `inline` ou `hybrid`, vérifiez que l'IA génère bien les patterns attendus

3. **Vérifier les erreurs GitLab API** : Les logs montrent maintenant les erreurs HTTP détaillées
   ```
   Failed to post inline comment on src/file.ts:42: HTTP 400 Bad Request
   Response body: {"message":"Invalid position"}
   ```

4. **Fallback automatique** : Si les SHAs manquent, le système poste automatiquement en mode `global`
   ```
   Missing commit SHAs, cannot post inline comments
   Posting as global comment instead
   ```

---

## 🚀 Exemples de configuration

### Configuration minimaliste (débutant)
```bash
COMMENT_MODE=global
AI_MODEL=claude-3-5-sonnet-20241022
```

### Configuration recommandée (production)
```bash
COMMENT_MODE=hybrid
AI_MODEL=claude-3-5-sonnet-20241022
```

### Configuration avancée (format strict)
```bash
COMMENT_MODE=structured
AI_MODEL=gpt-5
```

---

## 🔄 Migration depuis l'ancienne version

Si vous utilisez déjà ce projet, **rien à faire** ! Le mode par défaut est `global`, qui correspond au comportement actuel.

Pour activer les nouveaux modes :
1. Ajoutez `COMMENT_MODE=hybrid` dans votre `.env`
2. Redéployez l'application
3. Testez avec une nouvelle MR

Pas besoin de changer les webhooks GitLab existants.
